# Hurricane desk — Ara clips

Live pack is on disk:

`Media/public/audio/words/hurricane/` (283 WAVs from the Ara zip)

Index: `docs/hurricane-ara-pack/` (phrases.json, stems_and_slots.md)

Ava stitches those filenames plus existing number clips. Storm **names** are still one-off: `storm_<name>.wav` in the same folder (spoken name only). Until that file exists, the desk skips the name and still speaks class, distance, and watches.

Do not speak placeholder tokens. Do not call a storm a product.
