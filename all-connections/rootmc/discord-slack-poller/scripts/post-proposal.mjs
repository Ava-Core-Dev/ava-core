/**
 * Historical — original Sexi foundation proposal.
 * Live brand is Ava Ivy; runtime is Web Files/rootmc-ava.
 * Do not post this as-is.
 */
import { loadEnv, botToken, DISCORD_API } from "../src/config.mjs";
import fs from "node:fs";
import path from "node:path";
import { fileURLToPath } from "node:url";

const __dirname = path.dirname(fileURLToPath(import.meta.url));
console.error("post-proposal.mjs is archived. Use Ava Ivy docs under Server Handoffs/Ava Ivy.");
process.exit(1);
