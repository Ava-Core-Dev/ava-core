let EVENTS=[];const esc=s=>String(s??'').replace(/[&<>"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[c]));const fmt=v=>{try{return new Intl.DateTimeFormat(undefined,{dateStyle:'medium',timeStyle:'short'}).format(new Date(v))}catch{return v||'—'}};
function render(){const q=(document.getElementById('q')?.value||'').toLowerCase(),min=Number(document.getElementById('mag')?.value||0),out=EVENTS.filter(x=>(Number(x.magnitude)||0)>=min&&(`${x.place||''} ${x.country_code||''}`.toLowerCase().includes(q))).sort((a,b)=>(b.observed_at||'').localeCompare(a.observed_at||''));document.getElementById('tableWrap').innerHTML=`<div class="directory-tools"><input id="q" value="${esc(q)}" placeholder="Search place or country…"><select id="mag"><option value="0">All magnitudes</option><option value="2.5">M 2.5+</option><option value="4">M 4+</option><option value="5">M 5+</option><option value="6">M 6+</option></select></div><table class="directory-table"><thead><tr><th>Magnitude</th><th>Place</th><th>Time</th><th>Depth</th><th>Region</th></tr></thead><tbody>${out.map(x=>`<tr><td>M ${esc(x.magnitude??'—')}</td><td>${esc(x.place||'Unknown')}</td><td>${fmt(x.observed_at)}</td><td>${x.depth_km!=null?esc(x.depth_km)+' km':'—'}</td><td>${esc(x.country_code||'GLOBAL')}</td></tr>`).join('')||'<tr><td colspan="5">No events match.</td></tr>'}</tbody></table>`;document.getElementById('q').addEventListener('input',render);document.getElementById('mag').addEventListener('change',render);document.getElementById('mag').value=String(min)}
async function load(){try{const r=await fetch('/api/earthquakes/global',{cache:'no-store'});if(!r.ok)throw Error();const d=await r.json();EVENTS=d.events||[];render();document.querySelector('.status span').textContent='GLOBAL DIRECTORY ONLINE'}catch{document.querySelector('.status span').textContent='DIRECTORY DEGRADED';document.getElementById('tableWrap').innerHTML='<div class="empty">The global earthquake database is temporarily unavailable.</div>'}}load();
