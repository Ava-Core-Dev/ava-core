Boot order: weather / status / Kīlauea prelims first, then morning Boot Report
and day-board slots. AdSense/AdMob/account/governance still fire from origin
lifespan on their own timers. See `boot_prelims.py` then `day_board_boot.py`.
/`.
