# Add these routes to the existing Cloudflare tunnel ingress.
# The broadcast server is the single web origin for avaivy.cloud.
ingress:
  - hostname: avaivy.cloud
    service: http://127.0.0.1:8080
  - hostname: www.avaivy.cloud
    service: http://127.0.0.1:8080
