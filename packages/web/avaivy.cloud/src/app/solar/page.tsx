import StatusBoard from "@/components/StatusBoard";

export const metadata = {
  title: "Solar — Ava Ivy",
  description: "Live solar desk for the HI Pacific Solar Root Server.",
};

export default function SolarPage() {
  return <StatusBoard title="Solar" />;
}
